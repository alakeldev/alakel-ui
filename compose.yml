services:
  # UI Website (Docs with integrated Playground)
  syr-ui:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    container_name: syr-ui
    ports:
      - "3001:3001"
    command: npm run dev:syr-ui
