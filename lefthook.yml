# Lefthook Configuration
# Git hooks for code quality checks before commit/push

pre-commit:
  parallel: true
  commands:
    biome:
      run: npm run biome:fix
      stage_fixed: true
    typecheck:
      run: npm run tsc:check

pre-push:
  commands:
    build:
      run: npm run build
